# Variables de entorno para Nadro Mentoría Backend
# IMPORTANTE: Este archivo NO debe subirse a Git por seguridad

# Puerto del servidor
PORT=3001

# Entorno de ejecución
NODE_ENV=development

# JWT Secret - DEBE ser una cadena larga y aleatoria en producción
# Generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=tu-jwt-secret-super-seguro-aqui-minimo-32-caracteres

# Tiempo de expiración del JWT
JWT_EXPIRES_IN=7d

# Región de AWS
AWS_REGION=us-east-1

# Nombres de las tablas de DynamoDB
CONSULTAS_TABLE=NadroMentoria-Consultas
USUARIOS_TABLE=NadroMentoria-Usuarios

# Configuración de AWS (opcional si usas AWS CLI)
# AWS_ACCESS_KEY_ID=tu-access-key
# AWS_SECRET_ACCESS_KEY=tu-secret-key
